#!/bin/bash

# A script to start selenium browser for browser testing with behat

./vendor/bin/selenium-server-standalone --disable-dev-shm-usage -p 4444 -Dwebdriver.chrome.driver="./vendor/bin/chromedriver"
