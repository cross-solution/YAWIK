<?php
/**
 * YAWIK
 *
 * @filesource
 * @copyright https://yawik.org/COPYRIGHT.php
 * @license   MIT
 */

$this->headTitle($this->translate('Apply'));
$this->headscript()->appendFile($this->basepath('modules/Core/js/jquery.formcollection.js'));
//$this->headscript()->appendScript('$(function(){$(".form-collection").formcollection();});');
$this->headscript()->appendScript('$(".alert").alert();');
?>

<h1><?php echo $this->translate('Application')?></h1>
<p class="lead"><?php printf(
    $this->translate('Apply to "%s" from "%s"'),
    '<a href="'.$job->getLink().'" title="' . $this->translate('click to open the job ad') . '">' . $this->escapehtml(strip_tags($job->getTitle())) . '</a>',
    $job->getCompany()
) ?></p>

<?php if (!$isApplicationSaved):
    $form->setAttributes(array(
        'action' => $this->url('lang/apply/submit', array(), true),
        'method' => 'post',
        'id' => 'application-form',
    ));

    echo $this->form($form, Core\Form\View\Helper\Form::LAYOUT_HORIZONTAL);
endif; ?>
