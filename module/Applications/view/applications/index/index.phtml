<?php 
    $this->headTitle($this->translate('Apply'));
    
    $this->headLink()->appendStylesheet($this->basePath('/css/Core/forms.css'));
    
    $this->headScript()->appendFile($this->basePath('js/jquery.inlineLabel.js'))
                       ->appendFile($this->basepath('js/Applications/form.js'));
    
    
?>
<h2><?php printf(
    $this->translate('Apply to "%s" from "%s"'), 
    '<a href="/job-url" title="' . $this->translate('Go to job advertisement') . '">' . $this->escapehtml($job->title) . '</a>',
    'TestCompany'
) ?></h2>

<?php echo $isApplicationSaved ? 'Yeehaw!' : $this->partial('form/application', array('form' => $form)) ?>