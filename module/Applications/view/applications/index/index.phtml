<?php 
/**
 * Cross Applicant Management
 *
 * @filesource
 * @copyright (c) 2013 Cross Solution (http://cross-solution.de)
 * @license   AGPLv3
 */

$this->headTitle($this->translate('Apply'));
$this->headscript()->appendFile($this->basepath('Core/js/jquery.formcollection.js'));
$this->headscript()->appendScript('$(function(){$(".form-collection").formcollection();});');
$this->headscript()->appendScript('$(".alert").alert();');
?>
<h1><?php echo $this->translate('Application')?></h1>
<p class="lead"><?php printf(
    $this->translate('Apply to "%s" from "%s"'), 
    '<a href="'.$job->link.'" title="' . $this->translate('click to open the job ad') . '">' . $this->escapehtml(strip_tags($job->title)) . '</a>',
    $job->company
) ?></p>

<?php if ($isApplicationSaved):?>
<p class="alert alert-success">
 <?php echo $this->translate('your application was sent successfully')?>
 <a class="close" data-dismiss="alert" href="#" aria-hidden="true">&times;</a>
</p>
<?php else: 
    $form->setAttributes(array(
        'action' => $this->url('lang/apply/submit', array(), true),
        'method' => 'post',
        'id' => 'application-form',
    ));
        
    echo $this->form($form);
endif; ?>
 