<?php 
    $this->headTitle($this->translate('Apply'));
    
    $this->headLink()->appendStylesheet($this->basePath('/css/Core/forms.css'));
    
    $this->headScript()->appendScript('
        $(function () {
            $("fieldset > legend").click(function() {
                $("#" + $(this).parent().attr("id") + " .fieldset-content").slideToggle();
            });
        
            $(":text").inlineLabel();
        
            $("input[type=\'submit\'], button").button();
        });'
    )->appendFile($this->basePath('js/jquery.inlineLabel.js'));
    
?>
<h2><?php printf(
    $this->translate('Apply to "%s" from "%s"'), 
    '<a href="/job-url" title="' . $this->translate('Go to job advertisement') . '">' . $this->escapehtml($job->title) . '</a>',
    'TestCompany'
) ?></h2>

<?php echo $this->partial('form/application', array('form' => $form)) ?>