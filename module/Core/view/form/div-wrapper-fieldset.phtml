<?php
    $id = $element->getAttribute('id');
    if (!$id) {
        $id = uniqid();
    }
    
    $label = $element->getLabel();
    
?>
<div id="<?php echo $id?>" class="div-fieldset">
<? foreach ($element->getIterator() as $el): 
        echo $this->formPartial($el);
    endforeach ?>
</div>